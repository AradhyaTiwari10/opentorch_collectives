[project]
name = "supply-chain-sim"
version = "1.0.0"
description = "AI supply chain manager simulation."
requires-python = ">=3.10"
dependencies = [
    "openenv-core",
    "fastapi",
    "uvicorn[standard]",
    "pydantic>=2.0",
    "numpy",
    "scipy",
    "openai",
    "python-dotenv",
]

[tool.uv]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[project.scripts]
server = "server.app:main"
